/**
 * Куда уходят все формы сайта.
 *
 * Сайт статический и должен одинаково работать хоть на Netlify, хоть в S3,
 * поэтому адрес приёмника задаётся снаружи, а не зашит в разметку. Подойдёт
 * любой обработчик, принимающий POST: Formspree, Web3Forms, собственная
 * Lambda за API Gateway, Cloudflare Worker.
 *
 * Задаётся в .env:  PUBLIC_FORM_ENDPOINT="https://..."
 *
 * Переменная с префиксом PUBLIC_ попадает в клиентский бандл — это адрес,
 * а не секрет. Ключи отправителя писем должны остаться на стороне обработчика.
 */
export const formEndpoint = import.meta.env.PUBLIC_FORM_ENDPOINT ?? '';

/** Имя поля-ловушки: люди его не видят и не заполняют, боты заполняют. */
export const honeypot = 'company';
